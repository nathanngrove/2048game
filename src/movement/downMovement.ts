import { BEGINNING_OF_COLUMN, END_OF_COLUMN } from "../utils/constants";

const moveColumnDown = (board : HTMLElement, tile : number) => {
  //TODO
}

const combineColumn = (board : HTMLElement, row : number) => {
  //TODO
}

const combineTiles = (board : HTMLElement, row : number, firstTile : number, secondTile : number) => {
  //TODO
}

const canMoveDown = (board : HTMLElement) : boolean => {
  //TODO
}

export const moveTilesDown = (board : HTMLElement, row : number, tile : number) => {
  //TODO
}